#version 120

uniform int RenderRedChannel;
uniform int RenderGreenChannel;
uniform int RenderBlueChannel;

void main(){
	gl_FragColor = vec4(RenderRedChannel, RenderGreenChannel, RenderBlueChannel, 0.0f);
}